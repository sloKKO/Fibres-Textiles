@model fibresInTexilesMvc.ViewModels.FibAndProdViewModel

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>GetFiberProd</title>
</head>
<body>
    <table>
        <tr>
            <td>
                @Html.DisplayFor(f => f.fibre.Picture1)
        </td>
            <td>
                @Html.DisplayFor(f=>f.fibre.Description)
            </td>
            <td>
                @Html.DisplayFor(f=>f.fibre.Picture2)
            </td>
    </tr>
    </table>
    <h3>Products</h3>
    <table>
        @foreach(var product in Model.Products)
        {
            <tr>
                <td>
                    @*@Html.Image(@Model.Picture)*@
                    @product.Picture
                </td>
                <td>
                    @product.Name
                </td>
            </tr>
        }
    </table>
    @*<div> 
    </div>*@
</body>
</html>
